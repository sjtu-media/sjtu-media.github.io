services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    stdin_open: true
    tty: true
    working_dir: /usr/src/app
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/usr/src/app
